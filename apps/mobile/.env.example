# Copy to .env and fill after `npx eas-cli init`
EAS_PROJECT_ID=
# Optional Expo account owner (org slug)
EAS_OWNER=
# Optional first-party Worker URL prefilled on the server screen
EXPO_PUBLIC_DEFAULT_SERVER=https://mail.example.com
